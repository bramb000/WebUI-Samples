# Soft-gate password for Guild + Rocksmith deep case-study sections.
# Hero + executive summary stay public. Leave unset locally to keep locked with a config error on submit.
# Unlock persists across refresh only in production builds (`vite build`); `npm run dev` always starts locked.
VITE_CASE_STUDY_PASSWORD=
